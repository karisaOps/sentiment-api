name: Build, Ship, Deploy

on:
  push:
    branches: [ master ]

# Prevent concurrent deployments
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ DOCKER_CREDENTIALS.DOCKER_USERNAME }}
          password: ${{ DOCKER_CREDENTIALS.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            jkalume/sentiment-api:latest
            jkalume/sentiment-api:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Server
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ DOCKER_CREDENTIALS.SERVER_HOST }}
          username: ${{ DOCKER_CREDENTIALS.SERVER_USER }}
          key: ${{ DOCKER_CREDENTIALS.SSH_KEY }}
          script: |
            set -e

            IMAGE="jkalume/sentiment-api"
            TAG="${{ github.sha }}"

            echo "🔄 Pulling image $IMAGE:$TAG ..."
            docker pull $IMAGE:$TAG

            echo "🛑 Stopping old container (if running)..."
            docker stop sentiment-api || true

            echo "🗑️ Removing old container..."
            docker rm sentiment-api || true

            echo "🚀 Starting new container..."
            docker run -d \
              --restart unless-stopped \
              -p 5000:5000 \
              --name sentiment-api \
              $IMAGE:$TAG

            echo "✅ Deployment completed successfully! Container running on port 5000."